<!-- only iOS!! -->

<Alloy>
	<NavigationWindow class="container">
		<Window id="mainWindow" >
			<LeftNavButton>
				<Button id="accountButton" class="leftNavButton">acnt</Button>
			</LeftNavButton>
			<RightNavButton>
				<Button id="donateButton" class="rightNavButton" systemButton="COMPOSE"/>
			</RightNavButton>
			<!-- <TitleControl>
				<View id="navBarView" >
					<Button id="accountButton" class="leftNavButton">acnt</Button>
					<View id="titleView">
						<ImageView id="titleImageView" />
						<Label id="titleLabel" />
					</View>
					<Button id="donateButton" class="rightNavButton">donation</Button>
				</View>
			</TitleControl> -->
			<!-- <View id="navBarView" >
				<Button id="accountButton" class="leftNavButton">acnt</Button>
				<View id="titleView">
					<ImageView id="titleImageView" />
					<Label id="titleLabel" />
				</View>
				<Button id="donateButton" class="rightNavButton">donation</Button>
			</View> -->
			<Require id="userListView" src="userListView" />
		</Window>
	</NavigationWindow>
</Alloy>